server:
  port: 8011

spring:
  application:
    name: generator
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://119.91.63.227:3306/generator?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    driverClassName: com.mysql.cj.jdbc.Driver
    username: generator
    password: generator
    hikari:
      minimum-idle: 3
      max-lifetime: 18000

mybatis-plus:
  mapper-locations: classpath*:**/mapper/*/*.xml
  global-config:
    #刷新mapper 调试神器
    db-config:
      # 生成ID方式： AUTO 数据库自增  INPUT 自己输入  ASSIGN_UUID 默认生成器
      id-type: AUTO
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  type-aliases-package: com.moonlit.*
  #    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl











