server:
  port: 4749

spring:
  application:
    name: generator
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://127.0.0.1:3307/generator?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: qwe123

mybatis-plus:
  mapper-locations: classpath*:*/mapping/*.xml
  global-config:
    #刷新mapper 调试神器
    db-config:
      # 生成ID方式： AUTO 数据库自增  INPUT 自己输入  ASSIGN_UUID 默认生成器
      id-type: AUTO
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
#    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl
